// Selectores del DOM

const heading = document.querySelector('h2')
// Nos retorna unicamente un elemento 


const enlaces = document.querySelectorAll('.navegacion a')
// Nos retorna todas las coincidencias del selector

console.log(heading);
console.log(enlaces);

